// src/styles/fonts.ts
import { Inter, Roboto, Pathway_Gothic_One } from "next/font/google";

// Global font
export const inter = Inter({ subsets: ["latin"] });

// Roboto font for specific headings
export const pathway = Pathway_Gothic_One({ subsets: ["latin"], weight: "400" });
